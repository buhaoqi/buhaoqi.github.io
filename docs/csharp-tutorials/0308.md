---
# 这部分是关键！侧边栏显示名由这里决定
title: 3月8日 条件语句  # 文档标题，若无 sidebar_label 则作为侧边栏名
sidebar_label: 3月8日 条件语句  # 显式指定侧边栏显示名（优先级最高）
sidebar_position: 2  # 侧边栏中排在第1位
---


## 简易计算器

```csharp
// 欢迎语
Console.WriteLine("======欢迎使用简易计算器=======");

// 提示用户输入第一个数字
Console.Write("请输入第一个数字：");
// 读取用户输入的第一个数字
int num1 = int.Parse(Console.ReadLine());
// 提示用户输入第二个数字
Console.Write("请输入第二个数字：");
// 读取用户输入的第二个数字
int num2 = int.Parse(Console.ReadLine());
// 选择运算符
Console.WriteLine("请选择运算类型：+ - * /");
string fuHao  = Console.ReadLine();

// 判断符号
if (fuHao == "+")
{
    Console.WriteLine($"{num1} + {num2} = {num1 + num2}");
}

if (fuHao == "-")
{
    Console.WriteLine($"{num1} - {num2} = {num1 - num2}");
}

if (fuHao == "*")
{
    Console.WriteLine($"{num1} * {num2} = {num1 * num2}");
}

if (fuHao == "/")
{
    Console.WriteLine($"{num1} / {num2} = {num1 / num2}");
}
```

## dfdaf

```csharp
Console.WriteLine("===成绩等级查询器===");
Console.Write("输入分数：");
int score = int.Parse(Console.ReadLine());
// 如果 score >= 90 分 输出：优秀！
if (score >= 90)
{
    Console.WriteLine("优秀！");
}
else if (score < 90 && score >= 80)
{
    Console.WriteLine("中等");
}
else if (score < 80 && score >= 60)
{
    Console.WriteLine("及格");
}
else
{
    Console.WriteLine("未通过");
}
```