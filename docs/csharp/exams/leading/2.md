---
noteId: "5981126082e811f0b76dfd4874e1f79b"
tags: []

---


## 项目二 C#概述阶梯测试（一）
说明:本试卷满分100分,考试时间为45分钟

### 一、选择题(本大题35个小题,每小题2分,共70分)
1. 在C#中OOP代表的含义是（  ）  
A. 可视化编程  B. 面向对象  C. 面向过程  D. 命令结构  
2. 在C#程序中,用______和______开始和结束方法体。（  ）  
A. begin和end  B. if和endif  C. start和return  D. {和}  
3. 在C#程序中,每个语句必须以______作为语句的结束。（  ）  
A. 句号(.)  B. 分号(;)  C. 逗号(,)  D. 冒号(:)  
4. ______符号开始了一个单行注释。（  ）  
A. //  B. /*  C. */  D. note  
5. ______类可以显示消息对话框。（  ）  
A. Console  B. WriteLine  C. MessageBox  D. ReadLine  
6. C#在______方法处开始执行。（  ）  
A. Function  B. Main  C. main  D. Abstract  
7. 符号______开始了一个方法的定义体。（  ）  
A. {  B. }  C. ;  D.,  
8. C#程序用包含______的方法来指明将从某个命名空间里合并类。（  ）  
A. Main  B. main  C. Using  D. using  
9. C#中导入某一个命名空间的关键字是（  ）  
A. using  B. use  C. import  D. include  
10. 开发C#程序的集成开发环境是（  ）  
A. Visual Studio.Net  B. IDE  
C. FRAM  D. Common Language Runtime  
11. 以下标识符中,错误的是（  ）  
A. _b39  B. x345  C. 6_321  D. nAverage  
12. 结构化程序设计的3种结构是（  ）  
A. 顺序结构、if结构、for结构  
B. if结构、if...else结构、else if结构  
C. while结构、do...while结构、foreach结构  
D. 顺序结构、分支结构、循环结构  
13. Microsoft通过______向编程人员提供了功能强大的数据访问能力。（  ）  
A. ADO.NET  B. Windows.Form  C. XML  D. ASP.NET  
14. 下列语句在控制台上的输出是什么?（  ）  
```csharp
if(true)
System.Console.WriteLine("FirstMessage");
System.Console.WriteLine("SecondMessage");
```  
A. 无输出  B. FirstMessage  C. SecondMessage  D. FirstMessage  
15..NET框架是.NET战略的基础,是一种新的便捷的开发平台,它具有两个主要的组件,分别是______和类库。（  ）  
A. 公共语言运行库  B. Web服务  C. 命名空间  D. Main()函数  
16. 在C#中,下列代码的运行结果是（  ）  
```csharp
int[] age = new int[] {16,18,20,14,22};
foreach(int i in age) {
    if(i > 18)
        continue;
    Console.Write(i.ToString() + " ");
}
```  
A. 16,18,20,14,22  B. 16,18,14,22  C. 16,18,14  D. 16,18  
17. 在以下C#类中,______是控制台类,利用它我们可以方便的进行控制台的输入输出。（  ）  
A. Control  B. Console  C. Cancel  D. Write  
18. 在以下C#类中,______是控制台类,利用它我们可以方便的进行控制台的输入输出。（  ）  
A. Control  B. Console  C. Cancel  D. Writ  
19. C#语言中的类Console包含两个输出方法:Write()和WriteLine()。它们之间的唯一区别是（  ）  
A. WriteLine()方法输出后换行,Write()方法输出后不换行  
B. WriteLine()方法可以格式化输出,Write()方法不可以  
C. Write()方法输出后换行,WriteLine()方法输出后不换行  
D. Write()方法可以格式化输出,WriteLine()方法不可以  
20. 以下语句不可以在屏幕上输出Hello,World语句是（  ）  
A. Console.WriteLine("Hello" + ",World");  
B. Console.Write("Hello{0} ", "World");  
C. Console.WriteLine("{0},{1}", "Hello","World");  
D. Console.Write("Hello,World");  
21. 在.NET中,从属性的读写特性上来进行分类,可以划分为以下三种,除了（  ）  
A. 只读属性  B. 只写属性  C. 读写属性  D. 不可读不可写的属性  
22. 以下的C#代码:  
```csharp
static void Main(string[] args) {
    Console.WriteLine("运行结果: {0}",Console.ReadLine());
    Console.ReadLine();
}
```  
代码运行结果为（  ）  
A. 在控制台窗口显示“运行结果:”  
B. 在控制台窗口显示“运行结果: {0}”  
C. 在控制台窗口显示“运行结果: Console.ReadLine”  
D. 如果用户在控制台输入“A”,那么程序将在控制台显示“运行结果: A”  
23..NET中,程序中的错误可以划分为以下三类,除了（  ）  
A. 逻辑错误  B. 运行时错误  C. 语法错误  D. 自定义错误  
24. 在.NET中,一些数据类型为引用类型,当引用类型的值为______时,表明没有引用任何对象。（  ）  
A. Empty  B. null  C. Nothing  D. 0  
25. 在.NET中,程序员在代码中漏写了一个大括号,这属于（  ）  
A. 逻辑错误  B. 运行时错误  C. 语法错误  D. 自定义错误  
26. 请仔细阅读有如下C#代码:  
```csharp
int A = 3;
int B = 4;
Console.WriteLine(A + B);
```  
因为第二行代码少写了一个分号,程序运行时将（  ）  
A. 产生语法错误  B. 产生语义错误  C. 中断  D. 正常运行  
27. 在C#中,下列代码的运行结果是（  ）  
```csharp
int a = 30, b = 20;
b = a;
a = 10;
Console.WriteLine(a);
Console.WriteLine(b);
```  
A. 10 10  B. 10 30  C. 30 20  D. 10 20  
28. 在C#中,下列代码的运行结果是（  ）  
```csharp
string[] names = { "ACCP", "BENET", "BETEST" };
foreach(string name in names)
    Console.WriteLine(name);
```  
A. ACCP  B. n  C. name  D. "ACCP"  
29. 在.NET中,______类提供了操作字符串的方法。（  ）  
A. System.Threading  B. System.IO  
C. System.Collections  D. System.String  
30. 在C#中,下列代码的运行结果是（  ）  
```csharp
static void Main() {
    string name = "职教中心";
    foreach(char c in name) {
        Console.Write(c);
    }
}
```  
A. 输出name  B. 输出职教中心  C. 输出c c c c  D. 编译出错,存在语法错误  
31. 以下类MyClass的属性count属于______属性。  
```csharp
class MyClass {
    int i;
    int count {
        get { return i; }
    }
}
```  
A. 只读  B. 只写  C. 可读写  D. 不可读不可写  
32. 为了将字符串`str = "123456"`转换成整数123456,应该使用以下哪条语句?（  ）  
A. int Num = int.Parse(str);  
B. int Num = str.Parse(int);  
C. int Num = (int)str;  
D. int Num = int.Parse(str,Globalization.NumberStyles.AllowThousands);  
33. 关于C#程序的书写,下列不正确的说法是（  ）  
A. 区分大小写  
B. 一行可以写多条语句  
C. 一条语句可写成多行  
D. 一个类中只能有一个Main()方法,因此多个类中可以有多个Main()方法  
34. 在C#语言中,下列能够作为变量名的是（  ）  
A. if  B. 3ab  C. a_3b  D. a - bc  
35. 开发基于C#的Windows应用程序需要引用哪个名空间?（  ）  
A. System  B. System.Forms  
C. System.Collections  D. System.Data  

### 二、填空题(本大题10个小题,每空2分,共20分)
1. 要在控制台程序运行时输入信息,可使用Console类的______方法。  
2. 在C#中,进行注释有两种方法:使用______和使用______符号对,其中(//)只能进行单行注释。  
3. 当进入VS集成环境,如果没有显示“工具箱”窗口,应选择______菜单的“工具箱”选项,以显示工具箱窗口。  
4. Console.WriteLine("RP");和Console.Write("RP");的区别是______。  
5. 在C#程序中,程序的执行总是从______方法开始的。  
6. 结构化的程序设计的3种基本结构是______、______和______。  
7. C#程序的基本单位是______。  
8. C#语句末尾必须使用______。  
9. 下列语句在控制台上的输出是多少______。  
```csharp
string msg = @"Hello
World!";
System.Console.WriteLine(msg);
```  
10. 在C#中,用______开始和结束方法体。  

### 三、判断题(本大题5个小题,每小题2分,共10分)
1. C#中的void关键字用于表示方法没有返回值。（  ）  
2. C#中,Console.WriteLine可以输出到控制台。（  ）  
3. C#中,Null表示一个空对象引用。（  ）  
4. C#源代码的后缀名为.cs。（  ）  
5. 在使用变量之前必须声明类型。（  ）